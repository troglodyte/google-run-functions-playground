# Google run functions testing playground

### Notes
* `bin/` folder has http and pub/sub listener scripts.  
* Run either docker-compose up -d or use the `bin/run-docker.sh` file to use docker instead
* `test-calls.http` has some curl commands translated to Jetbrains .http calls
